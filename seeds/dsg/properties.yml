version: 2

seeds:
  - name: seed_dsg__category_data
    config:
      column_types:
        id: varchar
        name: varchar
        description: varchar
  - name: seed_dsg__store_data
    config:
      column_types:
        id: varchar(3)
        is_active: boolean
